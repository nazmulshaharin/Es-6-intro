//variable
var deposit = 500;
//condition 
if(deposit > 400){

}

// array
const numbers = [22, 25, 28, 15, 20, 35];
const numberCount = numbers.length;
numbers.push(111);
numbers.pop();
numbers[2] = 555;
// included in the array
if(numbers.indexOf(222) != -1){

}
if(numbers.includes(222)){
  
}